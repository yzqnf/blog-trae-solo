{
  "build": {
    "nodeVersion": "20.18.0",
    "installCommand": "npm install -g npm@9.8.1 && npm cache clean --force && rm -rf node_modules package-lock.json && npm install",
    "buildCommand": "npm run build"
  }
}
